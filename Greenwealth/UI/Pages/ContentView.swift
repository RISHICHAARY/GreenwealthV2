import SwiftUI

struct ContentView: View {
    
    @State var pageState = "LaunchPage"
    
    var body: some View {
        if(pageState == "LaunchPage"){
            
        }
    }
}

#Preview {
    ContentView()
}
